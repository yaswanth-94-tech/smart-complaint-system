# Render Blueprint for SCMS (100% FREE Tier Deployment)
services:
  # 1. Express Backend Web Service (FREE Tier)
  - type: web
    name: scms-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 5000
      - key: GEMINI_API_KEY
        sync: false
      - key: FRONTEND_URL
        fromService:
          type: web
          name: scms-frontend
          property: host
          prefix: https://

  # 2. React Frontend Static Site (FREE Tier)
  - type: web
    name: scms-frontend
    env: static
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: scms-backend
          property: host
          prefix: https://
          postfix: /api
      - key: VITE_FIREBASE_API_KEY
        sync: false
      - key: VITE_FIREBASE_AUTH_DOMAIN
        value: scms-hackathon.firebaseapp.com
      - key: VITE_FIREBASE_PROJECT_ID
        value: scms-hackathon
      - key: VITE_FIREBASE_STORAGE_BUCKET
        value: scms-hackathon.firebasestorage.app
      - key: VITE_FIREBASE_MESSAGING_SENDER_ID
        value: "67897379992"
      - key: VITE_FIREBASE_APP_ID
        value: "1:67897379992:web:d596aedcfc7af1f03514ed"
